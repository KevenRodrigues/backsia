<!DOCTYPE html>
<html lang="pt-br">
   <head>
      <title>Situação dos Exames</title>
      <link href="https://fonts.googleapis.com/css?family=Lato:400,700,900&display=swap" rel="stylesheet">
      <meta charset="utf-8" />
      <style>
         /** BASICA **/
         *, html {
         padding:0;
         margin:0;
         }
         html {
         font-family: 'Lato', Arial, sans-serif;
         }
         @media screen {
         body {
         display:flex; flex-direction:column;
         align-items: center;
         background-color: #ccc;
         }
         #wrap {
         background-color: #fff;
         padding: 50px;
         margin: 30px;
         box-shadow: 0px 0px 6px 1px rgba(0,0,0,0.25);
         }
         #loader{
         background-color: orangered;
         }
         .loader-hide {
         display:none;
         }
         .orientation-retrato{width: 60%;}
         .orientation-paisagem{width: 80%;}
         }
         @media print {
         html {
         border-color: #000;
         }
         p,span,h1,h2,h3,h4,h5 {
         color: #000 !important;
         }
         }
         h1, h2, h3, h4{
         font-weight: 900;
         }
         h1 {
         font-size: 15px;
         color: <%= color  %>;
         }
         h2 {
         font-size: 15px;
         color: <%= color  %>;
         padding-top: 10px;
         margin-top: 10px;
         border-top: 1px solid <%= color  %>;
         padding-bottom: 10px;
         margin-bottom: 10px;
         border-bottom: 1px solid <%= color  %>;
         text-transform: uppercase;
         }
         h3 {
         font-size: 12px;
         color: <%= color  %>;
         text-transform: uppercase;
         }
         strong {
         font-weight: 700;
         font-size: 12px;
         color: #444;
         }
         p {
         font-weight: 400;
         font-size: 12px;
         color: #444;
         text-transform: capitalize;
         }
         .row {
         display:flex;
         flex-direction: row;
         /* margin-bottom: 2px; */
         }
         /** HEADER **/
         header{
         display:flex;
         flex-direction: row;
         align-items:center;
         margin-bottom: 20px;
         }
         header div:first-child{
         flex:1
         }
         header h1 {
         font-size: 18px;
         text-transform: uppercase;
         margin-bottom: 3px;
         }
         header strong {
         margin-right: 3px;
         }
         header strong {
         margin-bottom: 3px;
         }
         header p, header strong {
         font-size:15px;
         }
         .logotipo {
         padding-left:50px
         }
         .logotipo img{max-height: 40px;}
         @media screen {
         .logotipo img{max-height: 60px;}
         }
         .legenda {
         padding-bottom: 15px;
         }
         .legenda p, .legenda p strong {
            font-size:15px;
         }
         .trans {
         display: flex;
         align-items: center;
         padding: 5px 0;
         border-bottom: 1px solid #ccc;
         }
         .exms {
         display: flex;
         align-items: center;
         padding: 5px 0;
         border-bottom: 1px dotted #ccc;
         }
         .exms-last {
         display: flex;
         align-items: center;
         padding: 5px 0 0 0;
         border-bottom: none;
         }
      </style>
   </head>
   <body>
      <%if (size === 'retrato') { %>
      <div id="wrap" class="orientation-retrato">
      <% } else { %>
      <div id="wrap" class="orientation-paisagem">
      <% } %>
      <!-- HEADER -->
      <header>
         <div>
            <h1>Situação dos Exames</h1>
            <p><strong>Período:</strong><%=start%> a <%=end%></p>
            <p class="emissao"><strong>Emissão:</strong><%=now%></p>
            <p class="operador"><strong>Operador:</strong><%=profile.nome%></p>
         </div>
         <%if (logo) { %>
         <div class="logotipo">
            <img src="<%= logo %>">
         </div>
         <% } %>
      </header>
      <!-- HEADER -->
      <!-- LEGENDA -->
    <div class="legenda">
        <p><strong>Legenda exames :</strong> <strong>+</strong> Falta Mat., <strong>#</strong> Falta Res., <strong>*</strong> Falta Imp., <strong>-</strong> Impresso, <strong>\</strong> Entregue</p>
    </div>
      <!-- FIM LEGENDA -->
      <!-- COMECO MAP SITUACAO DOS EXAMES -->
      <div class="data">
          <div class="row trans">
            <div style="width:10%">
                <strong>Data Entrada</strong>
            </div>
            <div style="width:6%">
                <strong>Posto</strong>
            </div>
            <div style="width:10%">
                <strong>Amostra</strong>
            </div>
            <div style="width:36%">
                <strong>Nome</strong>
            </div>
            <div style="width:40%">
                <strong>Exames</strong>
            </div>
        </div>

        <% data.forEach( (item,index,array) => {%>
            <% if (index === (array.length -1)) { %>
            <div class="row exms-last">
            <% } else { %>
            <div class="row exms">
            <% } %>
            <div style="width:10%">
                    <p><%= item.dataentra %></p>
                </div>
                <div style="width:6%">
                    <p><%= item.posto %></p>
                </div>
                <div style="width:10%">
                    <p><%= item.amostra %></p>
                </div>
                <div style="width:36%">
                    <p><%= item.nome %></p>
                </div>
                <div style="width:40%">
                    <p><%= item.exames %></p>
                </div>
            </div>
        <% }); %>
    </div>
</div>
    <!-- FIM MAP SITUACAO DOS EXAMES -->

   </body>
</html>
